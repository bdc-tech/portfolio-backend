"""config package for Django settings and WSGI/ASGI entry points.

Purpose:
 - Holds Django settings and app configuration for this project.
 - See `settings.py` for runtime configuration (REST framework, JWT, CORS).

This file is intentionally minimal.
"""

