"""Placeholder for app-specific Django tests.

Note: project-level pytest tests were added under `tests/` using pytest-django
for quick integration tests (see `tests/test_projects.py` and
`tests/test_auth.py`).
"""

from django.test import TestCase

# Create your tests here.
